import { appendPath, normalizeAccents } from '@planq-network/base/lib/string'
// Exports moved to @planq-network/base, forwarding them
// here for backwards compatibility
export { appendPath, normalizeAccents } from '@planq-network/base/lib/string'
export const StringUtils = {
  appendPath,
  normalizeAccents,
}
